const logger = require('../logger/logger'
const fs = require('fs')
const path = require('path')

//TODO: Get board member signatures
module.exports = async function(boardMemberIds: number[]) {
    console.log(boardMemberIds)
}